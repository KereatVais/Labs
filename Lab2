№1
#include "swap.h"

void Swap(char *left, char *right)
{
	char a;
    a = *left;
    *left = *right;
    *right = a;
}

№2
void RevertString(char *str)
{
	char *newStr
	for (int i = strlen(str) - 1; i > 0; i--)
        	*(newStr + strlen(i)) = *(str + strlen(str));
	*str = *newStr;
}
